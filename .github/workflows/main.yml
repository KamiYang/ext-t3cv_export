name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - name: Composer (php-actions)
        uses: docker://composer
        with:
          args: why typo3/cms
