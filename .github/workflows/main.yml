name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Composer (php-actions)
        uses: docker://composer
        with:
          volume: /home/runner/work/ext-t3cv_export:/app
          workdir: /app
          args: why typo3/cms
